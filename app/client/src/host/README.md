# Host tier

Application shell, dashboard canvas, settings, command surfaces, and rich editor host components live here. Widgets should not import this tier directly; compatibility shims at the old src root keep existing host imports stable during the transition.
